version: 2

sources:
  - name: raw
    database: WORKSPACE_SOURCE_DEV
    schema: COM_DEV

    tables:
      - name: hcp_master

      - name: zip_to_terr

      - name: ptnt_trans
   
      - name: hcp_addr