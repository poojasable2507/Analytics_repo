version: 2

sources:
  - name: raw
    database: WORKSPACE_SOURCE_DEV
    schema: COM_DEV

    tables:
      - name: hcp_master

      - name: ptnt_trans

      - name: zip_to_terr

      - name: hcp_addr

